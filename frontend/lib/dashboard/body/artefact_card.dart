import 'package:flutter/material.dart';

class ArtefactCard extends StatelessWidget {
  const ArtefactCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text('Artefact Card');
  }
}
